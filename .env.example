# Local development: leave empty so Vite uses /api and vite.config.js proxies to FastAPI.
# Vercel production: set this to your publicly reachable FastAPI URL, e.g. https://api.example.com
VITE_API_URL=
